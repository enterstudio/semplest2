@model IEnumerable<SemplestAdminApp.Models.UserRoleModel>
@{
    ViewBag.Title = "Create";
}
@section Title{
Role
}
@section Navbar{
    <script src="@Url.Content("~/Scripts/PanelBar.js")" type="text/javascript"></script>
}
@section Links{
    @Html.Partial("TopMenu")
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
        <div class="editor-label">
            Duplicate Rights of:
        </div>
        <div class="editor-field">
        @Html.DropDownList("ddl", ViewData["Roles"] as SelectList, new { onchange = "document.form.submit();" })  
        </div>
         <div class="editor-label">
            New Role: 
        </div>  
        <div class="editor-field">
       @Html.Editor("cc", "blah", new { @title = "true" })
        </div>
        @Html.Partial("_RolesRightsAssociation", Model)
        <p>
            <input type="submit" value="Create" />
        </p>
    <div>
    @Html.ActionLink("Back to List", "Index")
    </div>
    
 

}
   <script>
       //$("select").change(function () { var str = ""; $("select option:selected").each(function () { str += $(this).text() + " "; }); $("div").text(str); })
       //$("select").change(function () {
           //$.ajax({ type: "GET", url: "/Roles/Models", data: { RoleId: $(this).attr('value')} });
           //$.post("/Configuration", function(data) {   alert("Data Loaded: " + data)});
       //})
    </script>