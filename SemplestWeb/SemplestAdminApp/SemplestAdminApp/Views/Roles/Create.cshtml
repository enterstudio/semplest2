@model IEnumerable<SemplestAdminApp.Models.Right>
@{
    ViewBag.Title = "Create";
}
@section Title{
Role
}
@section Navbar{
    <script src="@Url.Content("~/Scripts/PanelBar.js")" type="text/javascript"></script>
}
@section Links{
    @Html.Partial("TopMenu")
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
         <div class="editor-label">
            New Role: 
        </div>  
        <div class="editor-field">
        <input name="blah" />
       @Html.Editor("cc",  "blah", new   { @title =  "true"})
        </div>
        <div class="editor-field">
            @*@Html.EditorFor(model => model.RoleName)*@
        </div>
        <div class="editor-label">
            Duplicate Rights of:
        </div>
        <div class="editor-field">
        @*Html.DropDownListFor(null, ViewData["Roles"], new { id = "RightPK" })*@
        @*Html.DropDownListFor(x => x.SelectedValue, ViewData["Roles"] as SelectList, new { id = "items" })*@
        @Html.DropDownList("ddl", ViewData["Roles"] as SelectList, new { onchange = "doSomething({Models}); return false;)" })  

         

            @*Html.DropDownList("Roles")*@
        </div>
    <table>
    <tr>
        <th>
            Page / Object
        </th>
    </tr>
        @foreach (var item in Model)
        {
    <tr>
        <td>
        @Html.CheckBox(item.RightsPK.ToString())@Html.Label(item.Controller)  @Html.Label(item.Label)
        
        </td>
    </tr>
        }
    </table>
        <p>
            <input type="submit" value="Create" />
        </p>
    <div>
    @Html.ActionLink("Back to List", "Index")
    </div>

}
