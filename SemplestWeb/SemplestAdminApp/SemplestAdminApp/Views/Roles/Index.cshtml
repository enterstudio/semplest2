@model IEnumerable<SemplestAdminApp.Models.Role>

@{
    ViewBag.Title = "Index";
}
<p>
    @Html.ActionLink("System Defaults", "../Configuration") |
    @Html.ActionLink("Security", "../Roles") 
</p>
<h2>Security</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table>
    <tr>
        <th>
            RoleName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
@*        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RolePK }) |
            @Html.ActionLink("Details", "Details", new { id=item.RolePK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RolePK })
        </td>*@
    </tr>
}

</table>
